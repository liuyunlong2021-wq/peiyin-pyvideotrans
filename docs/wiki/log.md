# Wiki Log

## 2026-08-01

- 执行 [[开发/产品收敛与本地部署精简SDD]]：去硬字幕改为外部独立仓库，制作项目由六阶段收敛为五阶段并可直接导入原视频识别。
- 默认依赖收敛到桌面主链；Qwen3-TTS 默认 0.6B，可单选 1.7B，首次使用只下载所选模型。全库 489 项测试通过。
- 当前 Git 跟踪内容约 19 MB；本地 `models/` 和 `.venv/` 均被忽略，不属于 GitHub 普通克隆体积。Windows 和全新机器真实短样片仍待目标环境验收。

## 2026-07-30

- 已从 `docs/` 提炼架构、开发、运维、排障和学习导航页；原始文档未移动或复制。
- 当前验证仅覆盖 Wiki 结构和链接，未覆盖应用运行、平台兼容性或外部服务。
- 新增 [[开发/韭菜盒子剧情翻译与逐句声音克隆SDD]]，明确固定 `gemini-3.6-flash`、整集上下文美式对白翻译、逐句原声克隆和最小接入边界；未修改韭菜盒子仓库。
- 已按 SDD 接入“韭菜盒子剧情翻译”和“韭菜盒子云端克隆”渠道及共用 API 设置；4 项模拟合同、Qt 离屏冒烟、编译与差异检查通过，真实付费调用仍待用户 Key 验证。
- 更新原 SDD 并实现人工发言轮次校对：字幕表新增“接上句”和段数摘要，保存为 `turns.json`；本地 Qwen3-TTS 优先服从人工分组，避免错误说话人识别把 A → B → A 合成同一人。

## 2026-07-31

- 执行 [[开发/Wiki驱动的分阶段配音制作SDD]] 步骤 0 至 3，新增独立制作项目入口、Wiki/Raw 项目骨架、集数状态看板和隔离工作目录的独立去字幕；真实媒体发布链路和 28 项测试通过，现有完整流程保持不变。
- 更新 [[开发/韭菜盒子剧情翻译与逐句声音克隆SDD]]，新增音频优先角色表演参考库设计：复用现有说话人分类，以 `emotion2vec+ large` 作为唯一新增情绪主模型并做同角色参考检索；SenseVoice 只兼容保留用户本来选择它时产生的标签。
- 明确首版只使用 JSON、WAV、NPY 和 NumPy 线性扫描，不引入向量数据库、MERTools、Mage-VL 主链路或独立人物管理页；所有新参考音必须人工批准后入库。
- 已执行音频优先角色参考库 SDD：复用 `speaker.json`，在现有校对窗口增加稳定人物、情绪与角色库确认，接入 emotion2vec+ large 和同角色参考检索；真实模型、UI 保存合同和禁止跨角色借音均已验证，待整集试听。
- 收紧并继续执行 [[开发/Wiki驱动的分阶段配音制作SDD]]：原 `TransCreate` 完整链路真实回归通过，独立项目模式的 SenseVoice 识别、取消摘要核对和两实例并行冒烟通过；新增发布前输入快照一致性校验。首集允许使用人工确认台词对应的本集原声初始化角色，制作项目继续作为实验入口。
- 修正分阶段状态一致性：项目模式的角色库处理失败不再静默回退；上游成功发布或重新确认后，下游状态统一失效，识别/翻译页的人工确认状态与项目看板同步。
- 补齐项目边界校验和异常恢复：严格校验导入/确认 SRT，精确提示空台词、倒置时间、空角色及首行接上句；打开项目发现遗留“处理中”时经人工确认后才允许标记失败重试。
- 外部原视频首次使用时归档到当前集 `.raw/media/视频/原视频.*` 并写入来源索引；同集已有不同原片时拒绝覆盖，避免项目迁移后失去源文件。
- 角色页创建推迟到人工确认导出，模型草稿不再污染角色库；已确认参考音增加情绪、参考文本、时长与音频流校验。
- 完成赚钱音浪 1.0.0 品牌化：统一启动图、主窗口、关于页、许可窗口、图标、版本和 Bundle ID；保留上游内部版本用于兼容。
- 新增 49 KB macOS 通用原生启动器，直接使用仓库 `.venv` 执行 `sp.py`；系统登记验证名称为“赚钱音浪”，不再显示 `python3.10`。
- 更新 [[运维/部署与入口]]、[[排障/常见问题]]、[[学习/项目概览]]、README 与 [[巡检报告/赚钱音浪1.0.0发布一致性检查]]，沉淀源码部署、品牌启动、根因与发布边界。
- 清理上游识别/UI/渠道重构后遗留的失效测试合同：旧 `stt_fun.py` 接口测试改为现行识别模块与工具行为测试，渠道编号、音高、任务默认值和 UI 断言同步当前实现；移除无文件、无调用的 OmniVoice 窗口注册。全库 490 项测试通过。
