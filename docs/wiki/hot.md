# 热缓存

> 更新：2026-08-01 | 阶段：产品收敛与本地部署精简

## 当前结论

- 任务编排、渠道注册、对齐、部署和排障入口已从 `docs/` 沉淀为导航页。
- 运行时渠道、模型和参数以代码注册表与本地配置为准，不能只依赖用户文档中的数量或版本说明。
- 已更新并执行 [[开发/韭菜盒子剧情翻译与逐句声音克隆SDD]]：翻译固定使用 `gemini-3.6-flash` 和整集 SRT 上下文；本地 Qwen3-TTS 按校对窗口人工确认的连续发言轮次克隆，字幕仍逐句保留。
- 已在同一 SDD 增加“音频优先角色表演参考库”：复用现有 `speaker.json`，`emotion2vec+ large` 是唯一新增情绪主模型；SenseVoice 仅兼容保留已有标签，不额外运行。
- 品牌已统一为“赚钱音浪 1.0.0”，macOS 原生启动器从仓库 `.venv` 启动现有代码，Dock、窗口、图标和 Bundle ID 一致；部署入口见 [[运维/部署与入口]]。
- 已执行 [[开发/产品收敛与本地部署精简SDD]]：主产品为五阶段桌面字幕制作链，去硬字幕使用外部独立仓库，默认安装不再包含历史本地模型渠道。

## 已验证 / 未验证

- 已验证：原有 4 项模拟合同、人工分组测试 2 项、校对窗口 Qt 离屏保存、Python 编译和差异检查通过。
- 已验证：真实短样片完成识别、剧情翻译、本地 Qwen3-TTS 1.7B 配音、对齐和最终合成；项目模式与原完整流程均保留。
- 已验证：清理上游重构后失效的测试合同并修正一条 OmniVoice 死窗口注册，全库 490 项自动测试通过。
- 已验证：精简后的锁文件可同步，全库 489 项自动测试通过；Qwen3-TTS 0.6B/1.7B 单选下载合同通过。
- 未验证：韭菜盒子声音克隆应用当前 `webappId`、节点字段、任务返回结构和实际音色效果仍需使用真实 Key 做一次小额测试。
- 已验证：emotion2vec+ large 本地推理输出 9 类分数与 1024 维 embedding；同角色长参考可供短句复用，无候选时不会跨角色借音。
- 未验证：真实整集的角色库参考与当前逐轮参考盲听 A/B；SenseVoice 标签兼容不属于首版必经链路。
- 未验证：Intel Mac、Windows、Linux、Docker、CUDA 和新机器首次模型下载；不可把 macOS Apple Silicon 结果外推为全平台结论。

## 下一步

- 用真实整集比较当前参考与角色库检索参考，重点听音色漂移、情绪贴合和短句开头瑕疵。
- 发布后让新用户按 README 从空环境执行一次 `uv sync` 和首次模型下载，记录平台差异。
